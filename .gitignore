# packaged bundles
*.zip

# working copies made from template/ or decks/ usually live next to the kit,
# but ignore the common scratch names just in case
my-presentation/

# credentials — never commit these
.env
.env.local

# OS noise
.DS_Store
